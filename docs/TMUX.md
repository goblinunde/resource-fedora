# 🚀 Tmux 终极配置指南

详细说明 `.tmux.conf` 配置文件的所有功能、快捷键和使用方法。

---

## 📋 目录

- [配置概览](#配置概览)
- [前置要求](#前置要求)
- [快速开始](#快速开始)
- [快捷键速查表](#快捷键速查表)
- [插件列表](#插件列表)
- [高级功能](#高级功能)
- [常见问题](#常见问题)

---

## 配置概览

**版本**: 3.0  
**前缀键**: `Ctrl+a` (替代默认的 `Ctrl+b`)  
**主题**: Dracula 配色  
**配置大小**: ~8KB  
**插件数量**: 10+

### 核心特性

- ✅ True Color (24位真彩色) 支持
- ✅ 鼠标操作支持
- ✅ Vi 风格快捷键
- ✅ 会话持久化 (自动保存/恢复)
- ✅ Vim/Neovim 无缝集成
- ✅ FZF 模糊搜索
- ✅ 浮动窗口 (Popup)
- ✅ 系统监控状态栏

---

## 前置要求

### 必需组件

```bash
# Tmux (≥ 3.0)
sudo dnf install -y tmux

# TPM (Tmux Plugin Manager)
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```

### 可选工具

```bash
# Git TUI 工具
sudo dnf install -y lazygit
# 或
cargo install gitui

# 系统监控
sudo dnf install -y htop btop

# 文件管理器
sudo dnf install -y ranger lf

# FZF (模糊搜索)
sudo dnf install -y fzf

# Python 开发
sudo dnf install -y python3 ipython
pip install jupyterlab
```

---

## 快速开始

### 1. 安装配置

```bash
# 部署 Tmux 配置
bash setup.sh --tmux

# 或手动复制
cp .tmux.conf ~/.tmux.conf
```

### 2. 安装插件

启动 Tmux 并按下：

```
Ctrl+a + I  (大写 i)
```

等待所有插件安装完成。

### 3. 重载配置

```
Ctrl+a + r
```

---

## 快捷键速查表

> **说明**: "前缀" 指 `Ctrl+a`

### 🎯 基础操作

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `前缀 + ?` | 显示所有快捷键 | 查看完整键位绑定 |
| `前缀 + r` | 重载配置 | 修改配置后立即生效 |
| `前缀 + :` | 命令模式 | 输入 Tmux 命令 |
| `前缀 + d` | 分离会话 | 后台运行 Tmux |

### 📝 窗口管理

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `前缀 + c` | 新建窗口 | 保持当前路径 |
| `前缀 + ,` | 重命名窗口 | 自定义窗口名 |
| `前缀 + &` | 关闭窗口 | 需确认 |
| `前缀 + X` | 强制关闭窗口 | 无需确认 |
| `前缀 + n` | 下一个窗口 | |
| `前缀 + p` | 上一个窗口 | |
| `前缀 + 1-9` | 切换到窗口 N | 直接跳转 |
| `前缀 + 0` | 切换到窗口 10 | |
| `Alt + h` | 上一个窗口 | 无需前缀 |
| `Alt + l` | 下一个窗口 | 无需前缀 |
| `Alt + 1-5` | 快速跳转窗口 | 无需前缀 |

### 🔲 面板管理

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `前缀 + v` | 垂直分割 | 左右分屏 |
| `前缀 + h` | 水平分割 | 上下分屏 |
| `前缀 + x` | 关闭面板 | |
| `前缀 + z` | 最大化/恢复面板 | Zoom 模式 |
| `前缀 + =` | 均匀分布面板 | |
| `前缀 + Space` | 循环切换布局 | |

#### 面板导航 (Vim 风格)

| 快捷键 | 功能 |
|--------|------|
| `前缀 + h/j/k/l` | 切换到 左/下/上/右 面板 |
| `前缀 + H/J/K/L` | 调整面板大小 (左/下/上/右) |

> **💡 提示**: 安装 `vim-tmux-navigator` 后可用 `Ctrl+h/j/k/l` 无缝切换 Vim 和 Tmux 面板

### 🐚 Shell 快捷键

| 快捷键 | 功能 | Shell |
|--------|------|-------|
| `前缀 + b` | 新建 Bash 窗口 | Bash |
| `前缀 + s` | 新建 Zsh 窗口 | Zsh |
| `前缀 + f` | 新建 Fish 窗口 | Fish |
| `前缀 + n` | 新建 Nushell 窗口 | Nushell |

### 🐍 Python 开发

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `前缀 + p` | Python 交互式 | `python3` |
| `前缀 + i` | IPython | 需安装 ipython |
| `前缀 + j` | Jupyter Lab | 需安装 jupyterlab |

### 🎈 浮动窗口 (Popup)

| 快捷键 | 功能 | 需要工具 |
|--------|------|----------|
| `前缀 + g` | Lazygit | lazygit |
| `前缀 + G` | Gitui | gitui |
| `前缀 + t` | 临时终端 | - |
| `前缀 + H` | Htop 系统监控 | htop |
| `前缀 + D` | 磁盘使用 | df |
| `前缀 + M` | 文件管理器 | ranger/lf |

### 📋 复制模式 (Vi 风格)

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `前缀 + [` | 进入复制模式 | Vi 操作 |
| `v` | 开始选择 | 复制模式内 |
| `V` | 行选择 | 复制模式内 |
| `Ctrl+v` | 矩形选择 | 复制模式内 |
| `y` | 复制并退出 | 复制到系统剪贴板 |
| `前缀 + ]` | 粘贴 | |
| `/` | 向前搜索 | Vi 风格 |
| `?` | 向后搜索 | Vi 风格 |
| `n` | 下一个匹配 | |
| `N` | 上一个匹配 | |

### 🔍 会话管理

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `前缀 + S` | 选择会话 | 树形视图 |
| `前缀 + W` | 选择窗口 | 树形视图 |
| `前缀 + f` | FZF 搜索 | 模糊搜索会话/窗口 |
| `前缀 + Ctrl+s` | 保存会话 | 手动保存 |
| `前缀 + Ctrl+r` | 恢复会话 | 手动恢复 |

### ⚙️ 高级功能

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `前缀 + E` | 同步模式 | 所有面板同步输入 |
| `前缀 + e` | Extrakto | 提取屏幕文本 |
| `前缀 + u` | 打开链接 | tmux-open 插件 |

---

## 插件列表

### 核心插件

| 插件 | 功能 | 必需 |
|------|------|------|
| [tpm](https://github.com/tmux-plugins/tpm) | 插件管理器 | ✅ |
| [tmux-sensible](https://github.com/tmux-plugins/tmux-sensible) | 合理的默认设置 | ✅ |

### 功能增强

| 插件 | 功能 | 快捷键 |
|------|------|--------|
| [vim-tmux-navigator](https://github.com/christoomey/vim-tmux-navigator) | Vim/Tmux 无缝导航 | `Ctrl+h/j/k/l` |
| [tmux-resurrect](https://github.com/tmux-plugins/tmux-resurrect) | 会话保存 | `前缀 + Ctrl+s/r` |
| [tmux-continuum](https://github.com/tmux-plugins/tmux-continuum) | 自动保存/恢复 | 自动 (10分钟) |
| [tmux-yank](https://github.com/tmux-plugins/tmux-yank) | 系统剪贴板集成 | `y` (复制模式) |

### 搜索与提取

| 插件 | 功能 | 快捷键 |
|------|------|--------|
| [tmux-fzf](https://github.com/sainnhe/tmux-fzf) | FZF 模糊搜索 | `前缀 + f` |
| [extrakto](https://github.com/laktak/extrakto) | 文本提取 | `前缀 + e` |
| [tmux-open](https://github.com/tmux-plugins/tmux-open) | 链接/文件打开 | `前缀 + u` |

### 状态栏

| 插件 | 功能 | 显示 |
|------|------|------|
| [tmux-cpu](https://github.com/tmux-plugins/tmux-cpu) | CPU/内存监控 | 状态栏右侧 |
| [tmux-battery](https://github.com/tmux-plugins/tmux-battery) | 电池状态 | 状态栏 (笔记本) |
| [tmux-prefix-highlight](https://github.com/tmux-plugins/tmux-prefix-highlight) | 前缀键提示 | 状态栏 |

---

## 高级功能

### 1. 会话持久化

#### Resurrect (手动保存/恢复)

```
# 保存会话
Ctrl+a + Ctrl+s

# 恢复会话
Ctrl+a + Ctrl+r
```

#### Continuum (自动保存)

- 每 10 分钟自动保存会话
- 启动时自动恢复上次会话
- 支持 Neovim 会话恢复

### 2. Vim/Neovim 集成

安装 `vim-tmux-navigator` 后，在 Vim 中添加：

```vim
" Vim 配置
Plug 'christoomey/vim-tmux-navigator'
```

**使用**:

- `Ctrl+h/j/k/l` 在 Vim 窗口和 Tmux 面板间无缝切换
- 无需区分是否在 Vim 中

### 3. FZF 模糊搜索

按 `前缀 + f` 打开 FZF 菜单：

- 搜索会话 (Session)
- 搜索窗口 (Window)
- 搜索面板 (Pane)
- 切换到命令历史

### 4. 浮动窗口工作流

**快速 Git 操作**:

```
前缀 + g  → Lazygit (可视化 Git 操作)
前缀 + G  → Gitui (Rust TUI)
```

**临时命令执行**:

```
前缀 + t  → 临时Shell (用完即关)
前缀 + H  → 系统监控
前缀 + D  → 磁盘使用
```

### 5. 文本提取 (Extrakto)

按 `前缀 + e`:

- 提取屏幕上的所有文本
- 支持: 路径、URL、IP地址、Git SHA
- FZF 模糊搜索
- 自动复制到剪贴板

---

## 常见问题

### Q: 如何更新插件？

```
Ctrl+a + U  (大写 U)
```

### Q: 为什么颜色显示不正确？

确保终端支持 True Color：

```bash
# 测试 True Color 支持
echo $TERM  # 应显示 xterm-256color 或 tmux-256color

# 在 .bashrc/.zshrc 中添加
export TERM=xterm-256color
```

### Q: Vim 中 Esc 键有延迟？

配置已设置 `escape-time 0`，如果仍有延迟：

```bash
# 检查配置
tmux show-options -g escape-time
```

### Q: 复制的文本无法粘贴到其他应用？

确保安装了剪贴板工具：

```bash
# Fedora/RHEL
sudo dnf install -y xclip xsel

# tmux-yank 会自动使用
```

### Q: 如何卸载不需要的插件？

1. 在 `.tmux.conf` 中删除或注释插件行
2. 按 `前缀 + Alt + u` 卸载插件

### Q: 会话恢复不工作？

检查插件状态：

```bash
# 查看 Resurrect 保存的会话
ls ~/.tmux/resurrect/

# 手动触发保存
tmux run-shell ~/.tmux/plugins/tmux-resurrect/scripts/save.sh
```

---

## 配置文件结构

```
.tmux.conf
├── 1. 核心基础设置
│   ├── 终端颜色与性能
│   ├── 交互体验
│   └── 索引与显示
├── 2. 快捷键配置
│   ├── 前缀键
│   ├── 窗口管理
│   ├── 面板管理
│   └── Shell/Python 快捷键
├── 3. 进阶功能
│   ├── 浮动窗口
│   ├── 会话管理
│   └── 批量操作
├── 4. 复制模式 (Vi)
├── 5. 界面美化
│   ├── 状态栏
│   └── 面板边框
├── 6. 插件管理 (TPM)
├── 7. 插件配置
└── 8. 初始化 TPM
```

---

## 性能优化

### 已优化项

- ✅ 历史记录限制: 100,000 行
- ✅ 状态栏更新间隔: 5 秒
- ✅ Esc 键延迟: 0ms
- ✅ 焦点事件: 启用
- ✅ 剪贴板集成: 启用

### 进一步优化

```tmux
# 添加到 .tmux.conf

# 禁用不需要的插件
# set -g @plugin '...'  # 注释掉

# 减少状态栏更新频率
set -g status-interval 10

# 限制历史记录 (如果内存紧张)
set -g history-limit 10000
```

---

## 推荐工作流

### 开发场景

```bash
# 1. 创建开发会话
tmux new -s dev

# 2. 窗口布局
# Window 1: 编辑器
Ctrl+a + c + 重命名为 "nvim"

# Window 2: 服务器
Ctrl+a + c + 重命名为 "server"

# Window 3: 测试
Ctrl+a + c + 重命名为 "test"

# 3. 面板分割
Ctrl+a + v  # 左右分屏
Ctrl+a + h  # 上下分屏

# 4. 保存会话
Ctrl+a + Ctrl+s
```

### 监控场景

```bash
# 面板1: htop (系统监控)
Ctrl+a + H

# 面板2: 日志查看
tail -f /var/log/app.log

# 面板3: 网络监控
watch -n 1 'netstat -tulpn'
```

---

## 参考资源

- [Tmux 官方文档](https://github.com/tmux/tmux/wiki)
- [Tmux Cheat Sheet](https://tmuxcheatsheet.com/)
- [TPM GitHub](https://github.com/tmux-plugins/tpm)
- [我的其他配置](../README.md)

---

**最后更新**: 2026-01-22  
**Tmux 版本**: 3.0+  
**作者**: SMLYFM <yytcjx@gmail.com>
