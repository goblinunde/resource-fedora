# ==============================================================================
# Tmux 终极配置文件 (Ultimate ~/.tmux.conf)
#
# 版本: 3.0 (集成 FZF, Popups, Lazygit, Vim-Nav)
# 前缀键: Ctrl + a
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. 核心基础设置 (Core Settings)
# ------------------------------------------------------------------------------

# --- 终端颜色与性能 ---
# 启用 256 色支持
set -g default-terminal "tmux-256color"
# 启用 True Color (24位真彩色) 支持，防止 vim 颜色失真
set-option -sa terminal-features ',xterm-256color:RGB'
set-option -sa terminal-overrides ",xterm*:Tc"
set-option -sa terminal-features ',alacritty:RGB,wezterm*:RGB'

# --- 交互体验 ---
# 启用鼠标 (点击切换面板、拖拽调整大小、滚轮滚动)
set -g mouse on
# 历史记录行数 (增加回滚缓冲区)
set -g history-limit 50000
# 消除 Esc 键在 Vim 中的延迟 (极重要！)
set -s escape-time 0
# 允许终端应用感知焦点变化 (对 Vim 的 autoread 很重要)
set -g focus-events on

# --- 索引与显示 ---
# 窗口和面板从 1 开始编号 (符合键盘数字键布局)
set -g base-index 1
setw -g pane-base-index 1
# 关闭窗口后自动重新排序编号
set -g renumber-windows on
# 自动重命名窗口
setw -g automatic-rename on
# 延长消息显示时间 (2秒)
set -g display-time 2000

# ------------------------------------------------------------------------------
# 2. 快捷键配置 (Keybindings)
# ------------------------------------------------------------------------------

# --- 前缀键 (Prefix) ---
# 修改为 Ctrl + a (更顺手)
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# 按两次 Ctrl+a 快速切换到上一个窗口
bind-key C-a last-window

# --- 配置文件重载 ---
bind r source-file ~/.tmux.conf \; display-message "🚀 Config reloaded!"

# --- 窗口与面板管理 ---
# v = 垂直分割 (左右双列) - 新面板保持当前路径
bind v split-window -h -c "#{pane_current_path}"
# h = 水平分割 (上下双行) - 新面板保持当前路径
bind h split-window -v -c "#{pane_current_path}"
# c = 创建新窗口 - 保持当前路径
bind c new-window -c "#{pane_current_path}"

# --- 定制的 Shell 快捷键 ---
bind b new-window -n "bash" "bash"
bind s new-window -n "zsh" "zsh"
bind f new-window -n "fish" "fish"
bind n new-window -n "nushell" "nu"

# --- Python 开发环境 ---
# 前缀 + p : 创建 Python 交互式窗口
bind p new-window -n "python" "python3"
# 前缀 + i : 创建 IPython 窗口
bind i new-window -n "ipython" "ipython"
# 前缀 + j : 创建 Jupyter Lab 窗口
bind j new-window -n "jupyter" "jupyter lab"

# --- 关闭操作 ---
bind x kill-pane        # 关闭当前面板
bind X kill-window      # 关闭当前窗口 (Shift+x)

# --- 导航 (Navigation) ---
# 1. 面板切换 (使用 Vim 风格 h,j,k,l)
bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R
# (注：安装 vim-tmux-navigator 插件后，可直接用 Ctrl-h/j/k/l 无缝切换)

# 2. 窗口切换 (Alt + 箭头/数字)
bind -n M-h previous-window
bind -n M-l next-window
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5

# 3. 调整面板大小 (Shift + H/J/K/L)
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# 4. 最大化/恢复面板 (Zoom)
bind z resize-pane -Z

# ------------------------------------------------------------------------------
# 3. 进阶功能与工作流 (Advanced Workflow)
# ------------------------------------------------------------------------------

# --- 浮动窗口工具 (Popup) ---
# 前缀 + g : 弹出 Lazygit (需安装 lazygit)
bind g display-popup -d '#{pane_current_path}' -w90% -h90% -E lazygit

# 前缀 + t : 弹出临时终端 (用于临时运行命令，用完即走)
bind t display-popup -d '#{pane_current_path}' -w80% -h80% -E $SHELL

# 前缀 + G : 弹出 Gitui (Git TUI 工具，需安装 gitui)
bind G display-popup -d '#{pane_current_path}' -w90% -h90% -E gitui

# 前缀 + H : 弹出 htop (系统监控)
bind H display-popup -w90% -h90% -E htop

# 前缀 + D : 弹出 d f (磁盘使用查看)
bind D display-popup -w80% -h80% -E 'df -h'

# 前缀 + M : 弹出文件管理器 (ranger/lf，需安装)
bind M display-popup -d '#{pane_current_path}' -w90% -h90% -E ranger

# --- 批量操作 ---
# 前缀 + E : 同步操作模式 (在一个面板输入，所有面板同时执行)
bind E setw synchronize-panes \; display "Sync mode: #{?synchronize-panes,ON,OFF}"

# --- 会话管理 ---
# 前缀 + S : 选择会话 (增强版)
bind S choose-tree -Zs
# 前缀 + W : 选择窗口
bind W choose-tree -Zw

# --- 快速跳转 ---
# 前缀 + 0 : 跳转到窗口 10
bind 0 select-window -t 10

# --- 布局管理 ---
# 前缀 + Space : 循环切换布局
bind Space next-layout
# 前缀 + = : 均匀分布面板
bind = select-layout tiled

# ------------------------------------------------------------------------------
# 4. 复制模式 (Copy Mode - Vi Style)
# ------------------------------------------------------------------------------
setw -g mode-keys vi

# 前缀 + [ 进入复制模式
# v 开始选择，y 复制 (由 tmux-yank 插件接管，复制到系统剪贴板)
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# ------------------------------------------------------------------------------
# 5. 界面美化 (UI & Theme)
# ------------------------------------------------------------------------------

# 状态栏位置
set -g status-position top
set -g status-bg '#282a36'
set -g status-fg '#f8f8f2'
set -g status-interval 1

# 左侧：会话名称
set -g status-left-length 60
set -g status-left '#[bg=#bd93f9,fg=#282a36,bold] ❐ #S #[fg=#bd93f9,bg=#282a36] '

# 中间：窗口列表
setw -g window-status-separator ''
setw -g window-status-format '#[fg=#6272a4,bg=#282a36]  #I:#W  '
setw -g window-status-current-format '#[fg=#282a36,bg=#ff79c6]#[fg=#282a36,bg=#ff79c6,bold] #I:#W #[fg=#ff79c6,bg=#282a36]'

# 右侧：系统信息 (依赖 tmux-cpu 插件)
set -g status-right-length 100
set -g status-right '#[fg=#6272a4,bg=#282a36]#[fg=black,bg=#6272a4] CPU: #{cpu_percentage}  MEM: #{ram_percentage} #[fg=#ff5555,bg=#6272a4]#[fg=black,bg=#ff5555] %Y-%m-%d %H:%M '

# 面板边框颜色
set -g pane-border-style fg='#6272a4'
set -g pane-active-border-style fg='#bd93f9'

# ------------------------------------------------------------------------------
# 6. 插件管理 (Plugins via TPM)
# ------------------------------------------------------------------------------

# 必须先手动克隆 TPM: 
# git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# --- 核心功能增强 ---
set -g @plugin 'christoomey/vim-tmux-navigator' # 完美整合 Vim/Tmux 导航 (Ctrl+h/j/k/l)
set -g @plugin 'tmux-plugins/tmux-resurrect'    # 会话保存
set -g @plugin 'tmux-plugins/tmux-continuum'    # 自动保存/恢复
set -g @plugin 'tmux-plugins/tmux-yank'         # 剪贴板复制

# --- 搜索与提取 (FZF & Extrakto) ---
set -g @plugin 'sainnhe/tmux-fzf'               # 模糊搜索 Session/Window
set -g @plugin 'laktak/extrakto'                # 快速提取屏幕文本 (Prefix + Tab)
set -g @plugin 'tmux-plugins/tmux-open'         # 打开高亮链接

# --- 状态栏组件 ---
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-battery'        # 电池状态 (笔记本)
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'  # 前缀键高亮

# --- 主题插件 (可选) ---
# set -g @plugin 'dracula/tmux'                    # Dracula 主题
# set -g @plugin 'nordtheme/tmux'                  # Nord 主题

# ------------------------------------------------------------------------------
# 7. 插件详细配置 (Plugin Configs)
# ------------------------------------------------------------------------------

# --- Resurrect & Continuum ---
set -g @continuum-restore 'on'            # 启动时自动恢复会话
set -g @continuum-save-interval '10'      # 每10分钟自动保存
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-nvim 'session' # 恢复 Neovim 会话

# --- Tmux FZF ---
# 绑定前缀 + f 呼出 FZF 菜单 (搜索会话、窗口等)
TMUX_FZF_LAUNCH_KEY="f"

# --- Extrakto ---
# 绑定前缀 + e 呼出屏幕文本提取
set -g @extrakto_key 'e'

# --- Battery (电池状态) ---
set -g @batt_icon_status_charged '🔋'
set -g @batt_icon_status_charging '⚡'
set -g @batt_icon_status_discharging '🔌'

# --- Prefix Highlight (前缀键提示) ---
set -g @prefix_highlight_fg 'black'
set -g @prefix_highlight_bg 'yellow'
set -g @prefix_highlight_show_copy_mode 'on'
set -g @prefix_highlight_copy_mode_attr 'fg=black,bg=yellow'

# --- 性能优化 ---
# 仅在可见窗口更新状态栏
set -g status-interval 5
# 限制面板历史记录（避免内存占用过大）
set -g history-limit 100000

# --- 自定义命令别名 ---
# 快速创建开发环境
# 用法: 前缀 + : dev <项目名>
bind-key -r D run-shell "tmux neww -c '#{pane_current_path}' -n dev"

# ------------------------------------------------------------------------------
# 9. 高级技巧与备注 (Advanced Tips)
# ------------------------------------------------------------------------------

# 💡 提示：
# 1. 安装 TPM: git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
# 2. 安装插件: 前缀 + I (大写 i)
# 3. 更新插件: 前缀 + U
# 4. 卸载插件: 前缀 + alt + u
#
# 前缀键组合速查:
#   Ctrl+a + ?

bash查看所有快捷键
#   Ctrl+a + r      重载配置
#   Ctrl+a + [      进入复制模式 (Vi 风格)
#   Ctrl+a + ]      粘贴
#
# Vim 集成:
#   安装 christoomey/vim-tmux-navigator 后
#   可用 Ctrl-h/j/k/l 无缝在 Vim 和 Tmux 间切换
#
# 会话持久化:
#   前缀 + Ctrl+s  手动保存会话
#   前缀 + Ctrl+r  手动恢复会话
#   (Continuum 插件会自动每 10 分钟保存)

# ------------------------------------------------------------------------------
# 8. 初始化插件管理器 (Keep this line at the very bottom)
# ------------------------------------------------------------------------------
run '~/.tmux/plugins/tpm/tpm'
